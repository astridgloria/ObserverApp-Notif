<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amiramaulina.gpstrackerapptrial1">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterEmailActivity"></activity>
        <activity android:name=".RegisterPasswordActivity" />
        <activity android:name=".RegisterNameActivity" />
        <activity android:name=".MenuAwal" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <activity android:name=".InviteCodeActivity" />
        <activity android:name=".LoginEmailActivity" />
        <activity android:name=".LoginPasswordActivity" />
        <activity android:name=".JoinCircleActivity" />
        <activity android:name=".MyCircleActivity" />
        <activity android:name=".LiveMapActivity" />
        <activity android:name=".MenuActivityUser" />
        <activity android:name=".FallHistory" />
        <activity android:name=".HRStatistics" />
        <activity android:name=".HRStateHistory" />



        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MyNavigationTutorial"
            android:label="GPS Tracker App Trial1"
            android:theme="@style/AppTheme" />
        <activity android:name=".MenuActivityUser"></activity>
    </application>

</manifest>